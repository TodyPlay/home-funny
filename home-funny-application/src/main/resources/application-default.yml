server:
  port: 8080
spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://${SERVER_IP}:3306/dev_jian
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: true
s3:
  endpoint: http://${SERVER_IP}:9000/
  access: minioadmin
  secret: minioadmin
  budget: video